language: java  
os: osx
jdk:
  - oraclejdk8
before_install: 
  - cd actor-apps/app-web
  - npm install
  - cd ../../
script:
  - # echo Building server... 
  - # cd actor-server
  - # sbt dist
  - # cd ..
  - echo Building web...
  - cd actor-apps/app-web
  - gulp build --release
  - cd ../../