package im.actor.core.util;

import org.junit.Test;

import im.actor.runtime.markdown.MDDocument;
import im.actor.runtime.markdown.MarkdownParser;

public class MarkdownTest {

    @Test
    public void testChineeseIssue() {
//        String input = "你在群里问的问题，说明你打开的不是ActorApp.xcworkspace";
//        MarkdownParser parser = new MarkdownParser(MarkdownParser.MODE_LITE);
//        MDDocument doc = parser.processDocument(input);
    }
}
