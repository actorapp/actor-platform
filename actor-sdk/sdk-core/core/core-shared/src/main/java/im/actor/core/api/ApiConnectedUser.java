package im.actor.core.api;
/*
 *  Generated by the Actor API Scheme generator.  DO NOT EDIT!
 */

import im.actor.runtime.bser.*;
import im.actor.runtime.collections.*;
import static im.actor.runtime.bser.Utils.*;
import im.actor.core.network.parser.*;
import org.jetbrains.annotations.Nullable;
import org.jetbrains.annotations.NotNull;
import com.google.j2objc.annotations.ObjectiveCName;
import java.io.IOException;
import java.util.List;
import java.util.ArrayList;

public class ApiConnectedUser extends BserObject {

    private int uid;
    private Boolean isConnecting;
    private Boolean isConnected;

    public ApiConnectedUser(int uid, @Nullable Boolean isConnecting, @Nullable Boolean isConnected) {
        this.uid = uid;
        this.isConnecting = isConnecting;
        this.isConnected = isConnected;
    }

    public ApiConnectedUser() {

    }

    public int getUid() {
        return this.uid;
    }

    @Nullable
    public Boolean isConnecting() {
        return this.isConnecting;
    }

    @Nullable
    public Boolean isConnected() {
        return this.isConnected;
    }

    @Override
    public void parse(BserValues values) throws IOException {
        this.uid = values.getInt(1);
        this.isConnecting = values.optBool(4);
        this.isConnected = values.optBool(5);
    }

    @Override
    public void serialize(BserWriter writer) throws IOException {
        writer.writeInt(1, this.uid);
        if (this.isConnecting != null) {
            writer.writeBool(4, this.isConnecting);
        }
        if (this.isConnected != null) {
            writer.writeBool(5, this.isConnected);
        }
    }

    @Override
    public String toString() {
        String res = "struct ConnectedUser{";
        res += "uid=" + this.uid;
        res += ", isConnecting=" + this.isConnecting;
        res += ", isConnected=" + this.isConnected;
        res += "}";
        return res;
    }

}
