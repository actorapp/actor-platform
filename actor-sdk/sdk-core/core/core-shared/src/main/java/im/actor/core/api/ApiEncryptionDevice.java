package im.actor.core.api;
/*
 *  Generated by the Actor API Scheme generator.  DO NOT EDIT!
 */

import im.actor.runtime.bser.*;
import im.actor.runtime.collections.*;

import static im.actor.runtime.bser.Utils.*;

import im.actor.core.network.parser.*;

import org.jetbrains.annotations.Nullable;
import org.jetbrains.annotations.NotNull;

import com.google.j2objc.annotations.ObjectiveCName;

import java.io.IOException;
import java.util.List;
import java.util.ArrayList;

public class ApiEncryptionDevice extends BserObject {

    private long deviceId;
    private List<ApiEncryptionKey> keys;

    public ApiEncryptionDevice(long deviceId, @NotNull List<ApiEncryptionKey> keys) {
        this.deviceId = deviceId;
        this.keys = keys;
    }

    public ApiEncryptionDevice() {

    }

    public long getDeviceId() {
        return this.deviceId;
    }

    @NotNull
    public List<ApiEncryptionKey> getKeys() {
        return this.keys;
    }

    @Override
    public void parse(BserValues values) throws IOException {
        this.deviceId = values.getLong(1);
        List<ApiEncryptionKey> _keys = new ArrayList<ApiEncryptionKey>();
        for (int i = 0; i < values.getRepeatedCount(2); i++) {
            _keys.add(new ApiEncryptionKey());
        }
        this.keys = values.getRepeatedObj(2, _keys);
    }

    @Override
    public void serialize(BserWriter writer) throws IOException {
        writer.writeLong(1, this.deviceId);
        writer.writeRepeatedObj(2, this.keys);
    }

    @Override
    public String toString() {
        String res = "struct EncryptionDevice{";
        res += "deviceId=" + this.deviceId;
        res += ", keys=" + this.keys;
        res += "}";
        return res;
    }

}
