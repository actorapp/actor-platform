package im.actor.core.api;
/*
 *  Generated by the Actor API Scheme generator.  DO NOT EDIT!
 */

import im.actor.runtime.bser.*;
import im.actor.runtime.collections.*;
import static im.actor.runtime.bser.Utils.*;
import im.actor.core.network.parser.*;
import org.jetbrains.annotations.Nullable;
import org.jetbrains.annotations.NotNull;
import com.google.j2objc.annotations.ObjectiveCName;
import java.io.IOException;
import java.util.List;
import java.util.ArrayList;

public class ApiEncryptedReceived extends ApiEncryptedContent {

    private int receiverId;
    private long receiveDate;

    public ApiEncryptedReceived(int receiverId, long receiveDate) {
        this.receiverId = receiverId;
        this.receiveDate = receiveDate;
    }

    public ApiEncryptedReceived() {

    }

    public int getHeader() {
        return 4;
    }

    public int getReceiverId() {
        return this.receiverId;
    }

    public long getReceiveDate() {
        return this.receiveDate;
    }

    @Override
    public void parse(BserValues values) throws IOException {
        this.receiverId = values.getInt(1);
        this.receiveDate = values.getLong(2);
    }

    @Override
    public void serialize(BserWriter writer) throws IOException {
        writer.writeInt(1, this.receiverId);
        writer.writeLong(2, this.receiveDate);
    }

    @Override
    public String toString() {
        String res = "struct EncryptedReceived{";
        res += "receiverId=" + this.receiverId;
        res += ", receiveDate=" + this.receiveDate;
        res += "}";
        return res;
    }

}
