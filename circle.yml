machine:
  java: 
    version: oraclejdk8

dependencies:
  pre:
    - actor-deps/installCoreDeps.sh
  cache_directories:
    - "actor-deps/dist"
  override:


test:
  pre:

  override:
    - ./gradlew build
  post:

