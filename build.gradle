allprojects {
    tasks.withType(JavaCompile).configureEach {
        options.fork = true
        options.incremental = true
    }


    repositories {
        jcenter()
    }

    buildscript {
        repositories {
            jcenter()
        }
    }
}