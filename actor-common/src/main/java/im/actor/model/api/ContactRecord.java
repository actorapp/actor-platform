package im.actor.model.api;
/*
 *  Generated by the Actor API Scheme generator.  DO NOT EDIT!
 */

import com.droidkit.bser.Bser;
import com.droidkit.bser.BserObject;
import com.droidkit.bser.BserValues;
import com.droidkit.bser.BserWriter;
import java.io.IOException;
import im.actor.model.network.parser.*;
import java.util.List;
import java.util.ArrayList;

public class ContactRecord extends BserObject {

    private int id;
    private long accessHash;
    private RecordType recordType;
    private String record;
    private String title;

    public ContactRecord(int id, long accessHash, RecordType recordType, String record, String title) {
        this.id = id;
        this.accessHash = accessHash;
        this.recordType = recordType;
        this.record = record;
        this.title = title;
    }

    public ContactRecord() {

    }

    public int getId() {
        return this.id;
    }

    public long getAccessHash() {
        return this.accessHash;
    }

    public RecordType getRecordType() {
        return this.recordType;
    }

    public String getRecord() {
        return this.record;
    }

    public String getTitle() {
        return this.title;
    }

    @Override
    public void parse(BserValues values) throws IOException {
        this.id = values.getInt(1);
        this.accessHash = values.getLong(2);
        this.recordType = RecordType.parse(values.getInt(3));
        this.record = values.getString(4);
        this.title = values.getString(5);
    }

    @Override
    public void serialize(BserWriter writer) throws IOException {
        writer.writeInt(1, this.id);
        writer.writeLong(2, this.accessHash);
        if (this.recordType == null) {
            throw new IOException();
        }
        writer.writeInt(3, this.recordType.getValue());
        if (this.record == null) {
            throw new IOException();
        }
        writer.writeString(4, this.record);
        if (this.title == null) {
            throw new IOException();
        }
        writer.writeString(5, this.title);
    }

}
