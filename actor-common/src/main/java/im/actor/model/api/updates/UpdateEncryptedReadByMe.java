package im.actor.model.api.updates;
/*
 *  Generated by the Actor API Scheme generator.  DO NOT EDIT!
 */

import im.actor.model.droidkit.bser.Bser;
import im.actor.model.droidkit.bser.BserObject;
import im.actor.model.droidkit.bser.BserValues;
import im.actor.model.droidkit.bser.BserWriter;
import static im.actor.model.droidkit.bser.Utils.*;
import java.io.IOException;
import im.actor.model.network.parser.*;
import java.util.List;
import java.util.ArrayList;
import im.actor.model.api.*;

public class UpdateEncryptedReadByMe extends Update {

    public static final int HEADER = 0x35;
    public static UpdateEncryptedReadByMe fromBytes(byte[] data) throws IOException {
        return Bser.parse(new UpdateEncryptedReadByMe(), data);
    }

    private Peer peer;
    private long rid;

    public UpdateEncryptedReadByMe(Peer peer, long rid) {
        this.peer = peer;
        this.rid = rid;
    }

    public UpdateEncryptedReadByMe() {

    }

    public Peer getPeer() {
        return this.peer;
    }

    public long getRid() {
        return this.rid;
    }

    @Override
    public void parse(BserValues values) throws IOException {
        this.peer = values.getObj(1, new Peer());
        this.rid = values.getLong(2);
    }

    @Override
    public void serialize(BserWriter writer) throws IOException {
        if (this.peer == null) {
            throw new IOException();
        }
        writer.writeObject(1, this.peer);
        writer.writeLong(2, this.rid);
    }

    @Override
    public String toString() {
        String res = "update EncryptedReadByMe{";
        res += "peer=" + this.peer;
        res += ", rid=" + this.rid;
        res += "}";
        return res;
    }

    @Override
    public int getHeaderKey() {
        return HEADER;
    }
}
