package im.actor.model.api.rpc;
/*
 *  Generated by the Actor API Scheme generator.  DO NOT EDIT!
 */

import im.actor.model.droidkit.bser.Bser;
import im.actor.model.droidkit.bser.BserParser;
import im.actor.model.droidkit.bser.BserObject;
import im.actor.model.droidkit.bser.BserValues;
import im.actor.model.droidkit.bser.BserWriter;
import im.actor.model.droidkit.bser.DataInput;
import im.actor.model.droidkit.bser.DataOutput;
import im.actor.model.droidkit.bser.util.SparseArray;
import org.jetbrains.annotations.Nullable;
import org.jetbrains.annotations.NotNull;
import com.google.j2objc.annotations.ObjectiveCName;
import static im.actor.model.droidkit.bser.Utils.*;
import java.io.IOException;
import im.actor.model.network.parser.*;
import java.util.List;
import java.util.ArrayList;
import im.actor.model.api.*;

public class ResponseJoinGroupDirect extends Response {

    public static final int HEADER = 0xc8;
    public static ResponseJoinGroupDirect fromBytes(byte[] data) throws IOException {
        return Bser.parse(new ResponseJoinGroupDirect(), data);
    }

    private Group group;
    private List<User> users;
    private long rid;
    private int seq;
    private byte[] state;
    private long date;

    public ResponseJoinGroupDirect(@NotNull Group group, @NotNull List<User> users, long rid, int seq, @NotNull byte[] state, long date) {
        this.group = group;
        this.users = users;
        this.rid = rid;
        this.seq = seq;
        this.state = state;
        this.date = date;
    }

    public ResponseJoinGroupDirect() {

    }

    @NotNull
    public Group getGroup() {
        return this.group;
    }

    @NotNull
    public List<User> getUsers() {
        return this.users;
    }

    public long getRid() {
        return this.rid;
    }

    public int getSeq() {
        return this.seq;
    }

    @NotNull
    public byte[] getState() {
        return this.state;
    }

    public long getDate() {
        return this.date;
    }

    @Override
    public void parse(BserValues values) throws IOException {
        this.group = values.getObj(1, new Group());
        List<User> _users = new ArrayList<User>();
        for (int i = 0; i < values.getRepeatedCount(2); i ++) {
            _users.add(new User());
        }
        this.users = values.getRepeatedObj(2, _users);
        this.rid = values.getLong(3);
        this.seq = values.getInt(4);
        this.state = values.getBytes(5);
        this.date = values.getLong(6);
    }

    @Override
    public void serialize(BserWriter writer) throws IOException {
        if (this.group == null) {
            throw new IOException();
        }
        writer.writeObject(1, this.group);
        writer.writeRepeatedObj(2, this.users);
        writer.writeLong(3, this.rid);
        writer.writeInt(4, this.seq);
        if (this.state == null) {
            throw new IOException();
        }
        writer.writeBytes(5, this.state);
        writer.writeLong(6, this.date);
    }

    @Override
    public String toString() {
        String res = "tuple JoinGroupDirect{";
        res += "}";
        return res;
    }

    @Override
    public int getHeaderKey() {
        return HEADER;
    }
}
