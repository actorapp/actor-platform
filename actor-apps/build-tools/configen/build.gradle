repositories {
    mavenCentral()
}

apply plugin: 'application'

mainClassName = "im.actor.config.Main"

run {
	if ( project.hasProperty("appArgs") ) {
		args Eval.me(appArgs)
	}
}

dependencies {
    compile "com.googlecode.json-simple:json-simple:1.1"
    compile "org.yaml:snakeyaml:1.16"
}